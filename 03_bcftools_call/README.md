# 03_bcftools_call: Legacy Archive

This stage is archived and no longer part of the active pipeline.

Use `03_gatk_call/` for all current runs.

Historical note
- this folder is retained only to document the older `bcftools` baseline
- the active autonomous runner, workflow, and downstream configs now target `03_gatk_call`
